#include "main.h"
/**
 * add - will return the sum of two integers
 * @x: is the first integer to be summed
 * @y: is the second integer to be summed
 * @z: is the value of the added integers
 * Return: 0 on success
 */
int add(int x, int y, int z)
{
	z = x + y;

	return (z);
}
