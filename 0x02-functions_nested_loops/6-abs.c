#include "main.h"
/**
 * _abs - will print the absolute value of an integer
 * @int: is the integer to get absolute value of
 * Return: 0 on success
 */
int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
